# Backend Environment Variables Template
# Copy this to .env and fill in your actual values

# Server Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# Frontend URL for CORS (update for production)
FRONTEND_URL=http://localhost:5173

# Database Connection
MONGO_URI=your_mongodb_connection_string_here

# Authentication Secret (generate a strong random string)
JWT_SECRET=your_jwt_secret_here

# For Vercel Production Deployment:
# - PORT: Vercel sets this automatically, don't override
# - HOST: Use 0.0.0.0 or omit (Vercel handles this)
# - NODE_ENV: Set to 'production' in Vercel dashboard
# - FRONTEND_URL: Set to your deployed frontend URL (e.g., https://your-app.vercel.app)
# - MONGO_URI: Use your MongoDB Atlas connection string
# - JWT_SECRET: Use a secure random string (min 32 characters)
